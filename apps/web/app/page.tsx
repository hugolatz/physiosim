import { Simulator } from '@/components/Simulator';

/**
 * The simulator is the landing page. A student opens the address and sees a body that is
 * already running — nothing to click through first.
 */
export default function Home() {
  return <Simulator />;
}
